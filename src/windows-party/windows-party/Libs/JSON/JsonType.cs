namespace OrletSoir.JSON
{
    /**
	 * Type enum - describes the native value type stored in an object.
	 */
    public enum JsonType
    {
        String,
        Int,
        Float,
        Bool,
        DateTime,
        Array,
        Set,
        Tuple
    }
}
