<UserControl x:Class="WindowsParty.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:controls="clr-namespace:WindowsParty.Controls"
		mc:Ignorable="d"
        d:DesignHeight="780" d:DesignWidth="1460"
        Height="Auto" Width="Auto">
	
	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="pack://application:,,/Styles/LoginScreenStyles.xaml"/>
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</UserControl.Resources>
	
	<Grid Background="{StaticResource LoginBackgroundPaint}">
		<StackPanel Style="{StaticResource ControlsArea}">
			<Image Style="{StaticResource LogoImage}"/>
			<controls:UsernameInput Username="{Binding Username}"/>
			<controls:PasswordInput PasswordValue="{Binding Password, Mode=TwoWay}" Margin="0, 20, 0, 0"/>
			<Button x:Name="LoginAsync" Content="Log in"  Style="{StaticResource LoginButton}"/>
		</StackPanel>
	</Grid>
</UserControl>
