<Window x:Class="Tesonet.Party.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Tesonet.Party.Controls"
        xmlns:views="clr-namespace:Tesonet.Party.Views"
        xmlns:viewModels="clr-namespace:Tesonet.Party.ViewModels"
        mc:Ignorable="d"
        Title="WOoohoo!!" Height="570" Width="1050"
         Foreground="#999999">

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModels:LoginVM}">
            <views:LoginView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:ServersVM}">
            <views:ServersView />
        </DataTemplate>
    </Window.Resources>
    
    <Grid>
        <ContentControl Content="{Binding SelectedViewModel}"/>
    </Grid>
</Window>
